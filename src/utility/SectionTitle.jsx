

const SectionTitle = ({children}) => {
    return (
        <div>
            <h1 className="text-black text-opacity-80 text-[32px] sm:text-[40px] font-nunito font-bold">{children}</h1>
        </div>
    );
};

export default SectionTitle;